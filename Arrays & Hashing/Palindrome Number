class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        z=str(x)
        y=z[::-1]
        if(z==y):
            return True
        else:
            return False
